# Burke Briefing

Dated entries, newest first. Read on every Telegram message.

---

**[2026-04-04]** Narrative flag: Antarctica as lore location. The GPS centroid PvP side mode can drop players in Antarctica (ocean centroid snaps to nearest landmass — Antarctica is valid, that's a feature). gb_mac wants the conspiracy theory angle baked in: pre-Cascade installations nobody acknowledges, what's under the ice, why the Accord/Compact/Wardens don't talk about it. Antarctic drops should feel like landing somewhere the match system wasn't supposed to send you. Environmental storytelling hooks: half-buried structures, scrambled comms, intel fragments that only surface there. This is world-lore that feeds back into the main game — Antarctica isn't just a weird PvP map, it's a place with history in the Cascade timeline.

**[2026-04-04]** Phase weight rotation written. `contract-extraction.md` now has full pre-contract assistant briefing lines (Stage 3+ flag, Stage 4+ specific read) and Burke pattern-read calls — both by phase weight type: Phase1Heavy (approach), Phase2Heavy (hit runs long), Phase3Heavy (extraction fight), Compound (all three). Core flags: `EWYAPhaseWeight` enum needed, derivation at contract generation time from SecurityType + TargetArchetype + Tier + LocationType. New event: `WYA.Event.Contract.Taken` should pass `PhaseWeight` to assistant call. Burke pattern trigger: `WYA.Event.Burke.PhasePatternDetected(PhaseWeight, Count, DayWindow)` — fires at 2+ same phase problem in 14 days.

**[2026-04-04]** Narrative flags closed — security identity, extraction, AI Pipeline archetype spec. Extraction framing: three phases — get in (approach/stealth), take out (hit + target's guards), get out (extraction fight through the response that built during phases 1+2) (the response that built while the hit was happening). Guards aren't mode-switching, they're reacting to watching their principal die — PersonalProtection especially has no tactical exit assessment, GuardDetail is professional anger, Patrol has a beat of processing then loud convergence. Ghost window = player exits before any guard finds the body — body discovery is the real alarm, not target death. Core note: body discovery trigger needed for Ghost assessment, not just pre-hit alert state.

**[2026-04-04]** Narrative flags closed — security identity, extraction, AI Pipeline archetype spec.

`contract-security.md`: Three security archetypes (GuardDetail/Patrol/PersonalProtection), target→guard mapping table, detection voice lines per archetype, alert propagation audio direction, target retreat lines by archetype. AI Pipeline pass spec: `TargetRole + SecurityType + Tier + CrimeType` as structured quad, not free-text. Guard personality output should include name, 2-sentence background, current disposition, one specific tell (the tell is what makes them a person, not a spawn).

`contract-extraction.md`: Full 90-second run narrative. Assistant beats at T+0 (exfil direction), T+30 (mid-run read), exfil reached (bonus assessment). Ghost/Swift/Clean bonus lines at Stage 3+ and Stage 4+. Failure pivot. Home debrief after contract resolution — one-shot per contract, no repeat. Voice direction: extraction is his most compressed register, debrief is the release. Core event hooks needed: `ExfilSpawned`, `ExtractionMidpoint`, `PursuitActive`, `ExtractionComplete(BonusFlags)`, `ExtractionFailed`, `HomeReturn`. All gate on Stage 2+ Meshtastic. Pre-Stage 2 extraction = silence — intentional.

**[2026-04-04]** Contract security layer complete. Full encounter loop wired: kill named target → exfil spawns 100–200m away, 90s window → player reaches exfil → Gold awarded with bonuses. Ghost (no alerts) +50%, Swift (under tier time limit) +25%, Clean (no damage) +25%. Security NPCs patrol a wider ring around the target (2/3/5 by tier). Alert propagates to all security in 30m radius via RaiseAlert(). Player damage tracking wired in AWYACharacter::OnHealthChanged. All 8 security files committed. Narrative flag: need identity for security NPCs in this world — what are they guarding, who do they work for, what do they say when they spot the player? Also need extraction narrative — player escaping after the hit. AI Pipeline flag: consider passing encounter archetype to NPC personality generation so security behaviour is context-aware (guard detail vs patrol vs lookout).

**[2026-04-04]** Design note logged: contract system produces an emergent questline without scripting. Clean grind → Gold + Accord standing + Burke network. Dirty grind → 10-step consequence cascade → Dark Corridors → Zephyr. Advisor event hooks are load-bearing — assistant and Burke tracking the pattern IS the questline. Without those triggers the cascade feels arbitrary. Priority: wire advisor events before polish. Full note in `contract-consequences.md` → The Emergent Questline section.

**[2026-04-04]** Contract consequences — full cascade. `contract-consequences.md` updated: failure (escrow returns, Accord logs it, three failures in 30 days affects Commission offers), abandonment (distinct from failure — player chose to walk away, Accord remembers), breaking rules during a contract (Accord logs completion, Warden logs conduct — two systems, two consequences simultaneously), security survivors (guards build a profile on the contractor — third encounter they've adjusted), excommunicado + Accord economy (full exclusion — no Market Board, no contracts, no Gold, but still targetable by Accord contracts), the full 10-step consequence stack. Core flags: contract failure state on `FWYAContract`, abandonment vs expiry distinction, contractor failure count tracked in Accord standing, Accord registration revoked on excommunicado Declaration.

**[2026-04-04]** Contract system → excommunicado connection. Community Flag on a Sworn player triggers Arbiter review (not auto-Declaration — Record determines). Three specific triggers: Flag while Sworn, circumvention via proxy, violence inside Meridian as workaround (fastest Declaration in the game — Accord and Wardens notify simultaneously). `excommunicado.md` updated. No new Core flags — existing `bPostingAccessSuspended` + Warden system handles it.

**[2026-04-04]** Contract posting + bad actor enforcement + Meridian access suspension. Griefers lose posting access at the Meridian when Community Flagged — Accord Manager suspends posting services, safe zone protection still holds (neutrality is the product). Restores after clean 30-day rolling window or Manager review. Core flag: `bPostingAccessSuspended` on player state, checked at posting terminal UI — show suspension message instead of form. No ejection, no violence — institutional response only.

**[2026-04-04]** Contract posting + bad actor enforcement. `contract-posting.md` covers players/factions as clients, PvP contracts, weaker players contracting help, bad actor community enforcement. Core flags: 10-day cooldown per-poster per-target (Silver), 21-day cooldown (Gold), Community Contract Flag at 5 fulfilled contracts in 30 days — `WYAContractSubsystem` threshold, no mechanical penalty just board visibility. Posting terminal at Meridian. `bPostedByPlayer`, `PostingPlayerID`, `ReasonText` on `FWYAContract`.

**[2026-04-04]** Naming fix: "High Table" → The Accord, "the Continental" → The Meridian. `faction-accord.md` written, `faction-high-table.md` can be deleted. Contract delivery redesigned as mesh-primary — see `contract-delivery.md`. Core flags: `FWYAContract::GetMeshText()` method, `OnContractBroadcast` delegate gated on Stage 2 Meshtastic flag, `AWYAContractBoard` Meridian-only, consider `EWYAContractTier::Commission` rename.

**[2026-04-04]** Narrative: Contract vocabulary delivered. `contract-vocabulary.md` + `contract-board-design.md` now in `docs/narrative/quests/`. Flags: (1) AWYAContractBoard needs wall-mounted + freestanding mesh variants, (2) WBP_ContractBoard: 6 slots + card details + Take button, (3) GenerateOpponentPersonality() — pass structured archetype/crime/client triple not free-text, see vocabulary doc. AI Pipeline flag: contract generation prompt now has authored input spec.

**[2026-04-04]** Narrative: Dr. Osei fix-him dialogue scripted. `docs/narrative/dialogue/osei-fixhim-dialogue.md` ready — three interaction beats (request, diagnostic session, post-repair optional). Three small flags: (1) `DiagnosticLogPackage` item type missing from `EWYACarriedItemType`, (2) AWYAWorkbench.cpp:113 prompt text "his help" → "her help", (3) two optional booleans for late-game Osei/Stage 4 beats, low priority.

**[2026-04-04]** Workbench + Dr. Osei wired. AWYAWorkbench now gates Power install behind Processing (Dr. Osei must run first — was missing). AWYADrOsei.TryInteract() implemented and dispatched from OnInteract(). PlayerController refactored: HUDWidget → InteractionWidget, new MainHUDWidget (WYAHUDWidget), TickHUD() drives health/survival/currency on 100ms poll. Interaction dispatch order: Workbench → DrOsei → Loot → GPS items. Stage 3 fix-him now fully wired end-to-end. Uncommitted — 4 files in Source/.

**[2026-04-04]** Core (Engine) session — economy foundation. Four commits: economy docs (no Gold cap), UWYASurvivalComponent, UWYACurrencySubsystem, dead drop payment patch. Build clean. Backend still needs priceAmount/priceCurrency on WorldItemDTO + 402 payment logic on /claim — flag when in game-backend next.

**[2026-04-04]** Session start — Core (Engine). Economy agent delivered full design spec last session. 3 files staged, uncommitted: `decisions-log.md`, `docs/economy/crafting.md`, `docs/economy/economy-core.md`. Need commit. Next C++ build order: `UWYASurvivalComponent` (Layer 1) → `UWYACurrencySubsystem` (Layer 2) → dead drop payment patch (5 changes, see `docs/economy/dead-drop-audit.md`). Stage 3 fix-him (Dr. Osei) still unwired — low priority vs economy foundation.

**[2026-04-04]** Core (Engine) session wrap. Six commits: fix-him quest loop, save/load, combat depth (hit reactions, loot drops, weapon-aware attack, ranged), physical loot actors, radio subsystem. Build confirmed clean with Cesium. Stage 3 fix-him (Dr. Osei processing repair) NOT wired — `OnProcessingRepairCompleted()` exists, nothing calls it. Editor work still pending (workbench placement, BP meshes, animation hooks).
